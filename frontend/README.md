# LandIQ AI - Frontend (mock)

This is a Vite + React + Tailwind mock frontend implementing the requested screens with mock data.

Quick start:

```powershell
cd frontend
npm install
npm run dev
```

Notes:
- Uses mock data only. No backend integration.
- Pages: Dashboard, Analytics, Fraud Detection, Property Analysis, Reports, Landing.
